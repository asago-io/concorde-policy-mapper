name: Security Scan

on:
  push:
  pull_request:

jobs:
  codeql:
    uses: project-concorde/concorde-infra/.github/workflows/reusable-codeql.yaml@main
    permissions:
      security-events: write
      actions: read
      contents: read
